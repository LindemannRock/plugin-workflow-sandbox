# Quickstart

> **Main / default docs** — served at this version's docs route.

1. Install the plugin.
2. Open it in the Control Panel.
3. Confirm this is the **Main / default docs** copy — proving the version-pinned route returned the right branch's docs.
